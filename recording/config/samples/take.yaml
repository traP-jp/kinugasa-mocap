apiVersion: recording.kinugasa.dev/v1alpha1
kind: Take
metadata:
  name: take-001
spec:
  streamRef:
    name: studio-a
  output:
    s3:
      bucket: mocap-recordings
      objectKey: takes/take-001.mp4
      region: ap-northeast-1
      secretRef:
        name: s3-credentials
